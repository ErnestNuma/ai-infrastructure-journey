# System Telemetry Engine (v1.0)
A Python-based utility that extracts raw system health data (CPU, Uptime) and parses it into structured JSON for infrastructure monitoring.# System Telemetry Engine (v1.0)
A Python-based utility that extracts raw system health data (CPU, Uptime) and parses it into structured JSON for infrastructure monitoring.
